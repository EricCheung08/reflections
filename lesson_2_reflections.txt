What happens when you initialize a repository? Why do you need to do it?
	It creates a .git file and let the system know that it's a git that we would like to track.


How is the staging area different from the working directory and the repository?
	Staging area is the intermediate stage. It should be a place to hold files you wish to update. Working directory can have any changes you like.


What value do you think it offers?
	Staging area allows for final approval before putting it into repository. It is a final check. 

How can you use the staging area to make sure you have one commit per logical
change?
	We can do this by keeping chnages small in size.


What are some situations when branches would be helpful in keeping your history
organized? How would branches help?
	I can continue trying new features without problems. Wheneever I am moving to a different piece of work. I like to think of it as context switching.  Branches is good if I'm bored of a feature. 

How do the diagrams help you visualize the branch structure?
	It makes it easier to tell between different parents.

What is the result of merging two branches together? Why do we represent it in
the diagram the way we do?
	We combined changes that two people are working on into one. We present it this way to indicate the parents or both are inherited.

What are the pros and cons of Git’s automatic merging vs. always doing merges
manually?
	Doing merges manually might take a long time. Staging  the first thing - make sure the commmit are the things you actually want to commit. 

Created a git repositiory. learnted to make parrell versions of the code. 